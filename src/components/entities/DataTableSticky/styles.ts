import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  root: {
    maxWidth: "90%",
    margin: "0 auto",
    marginTop: "30px",
    maxHeight: "700px",
    padding: "30px",
  },
  container: {
    maxHeight: 540,
  },
  button: {
    textTransform: "none",
  },
  input: {
    fontSize: "12px",
  },
});
